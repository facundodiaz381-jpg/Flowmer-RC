// Custom hooks reutilizables del proyecto
// Agregar acá los hooks que el equipo vaya creando
// Ejemplos comunes para este proyecto:
//   export { useWishlist } from './useWishlist'    → lógica de la wishlist del usuario
//   export { useVotes } from './useVotes'          → lógica de votos por juego
//   export { useDebounce } from './useDebounce'    → retrasar búsquedas en SearchBar
//   export { useLocalStorage } from './useLocalStorage' → abstracción de localStorage

export {}
